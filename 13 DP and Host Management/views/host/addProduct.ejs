<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Product</title>
  <link rel="stylesheet" href="/addProduct.css" />
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="container">
    <h1><%= edit ? 'Edit Product' : 'Add a New Product' %></h1>
    <form action="/host/<%= edit ? 'editProduct/' + product.id : 'add-product' %>" method="POST" class="product-form">
      <% if (edit) { %>
        <input type="hidden" name="id" value="<%= product.id %>">
      <% } %>
      <div class="form-group">
        <label for="name">Product Name</label>
        <input type="text" name="name" id="name" value="<%= edit ? product.name : '' %>" required placeholder="Enter product name" />
      </div>

      <div class="form-group">
        <label for="price">Price (₹)</label>
        <input type="text" name="price" id="price" value="<%= edit ? product.price : '' %>" required placeholder="Enter price" />
      </div>

      <div class="form-group">
        <label for="description">Description</label>
        <input type="text" name="description" id="description" value="<%= edit ? product.description : '' %>" required placeholder="Write a short description" />
      </div>

      <div class="form-group">
        <label for="image">Image URL</label>
        <input type="text" name="image" id="image" value="<%= edit ? product.image : '' %>" required placeholder="Paste image URL" />
      </div>

      <div class="form-group">
        <label for="rating">Rating</label>
        <input type="text" name="rating" id="rating" value="<%= edit ? product.rating : '' %>" required placeholder="e.g., 4.5" />
      </div>

      <button type="submit" class="btn"><%= edit ? 'Update Product' : 'Add Product' %></button>
    </form>
  </div>
</body>
</html>
