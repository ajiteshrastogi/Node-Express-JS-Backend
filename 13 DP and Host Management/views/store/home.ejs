<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Mart</title>
  <link rel="stylesheet" href="/home.css" />
</head>
<body>
  <%- include('../partials/navbar') %> <!-- Navbar Partial -->

  <section class="hero">
    <h1>Welcome to <span>Mart</span></h1>
    <p>The ultimate place to explore, shop, and host premium products.</p>
  </section>

  <main>
    <section class="products">
      <h2>Our Products</h2>
      <div class="product-container">
        <% registeredProducts.forEach(product => { %>
          <div class="product-card">
            <img src="<%= product.image || '/default-image.jpg' %>" alt="<%= product.name %>">
            <div class="product-info">
              <h3><%= product.name %></h3>
              <p class="description"><%= product.description %></p>
              <p class="price">₹<%= product.price %></p>
              <p class="rating">⭐ <%= product.rating %></p>
            </div>
            <div class="button-group">
              <a href="#" class="btn add-to-cart">Add to Cart</a>
              <a href="/product/<%= product.id %>" class="btn details-button">Details</a>
            </div>
          </div>
        <% }) %>
      </div>
    </section>
  </main>
</body>
</html>
